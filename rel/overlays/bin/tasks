#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

exec ./aurora_gov_web eval AuroraGovWeb.Release.drop
exec ./aurora_gov_web eval AuroraGovWeb.Release.migrate
exec ./aurora_gov_web eval AuroraGovWeb.Release.seed general
