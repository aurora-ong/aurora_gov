#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

seed_file="seeds"

./aurora_gov_web eval AuroraGov.Release.db_reset
./aurora_gov_web eval AuroraGov.Release.db_migrate
./aurora_gov_web eval "AuroraGov.Release.db_seed(\"$seed_file\")"