#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

seed_file="seeds"

./aurora_gov_web eval AuroraGov.Release.reset
./aurora_gov_web eval AuroraGov.Release.migrate
./aurora_gov_web eval "AuroraGov.Release.seed(\"$seed_file\")"